# Pinned to the versions validated in the cgn_torch conda env (2026-06-11).
# torch/torchvision are installed separately in the Dockerfile (cu128 index).
numpy==1.26.4
scipy==1.15.3
mujoco==3.9.0
open3d==0.19.0
opencv-python-headless==4.11.0.86
trimesh==4.12.2
imageio==2.37.3
pyyaml==6.0.3
pillow==12.2.0
tqdm
matplotlib
huggingface_hub
# pyrender deps (pyrender itself is installed --no-deps in the Dockerfile:
# its pinned PyOpenGL==3.1.0 would break MuJoCo's EGL backend)
freetype-py
pyglet
six
