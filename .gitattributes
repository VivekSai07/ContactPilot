# Protocol-0 ASCII pickle: git's text heuristic misclassifies this as text,
# and autocrlf=true (Windows default) injects CRLF on checkout, corrupting it
# (UnpicklingError: the STRING opcode argument must be quoted). Force binary
# so it survives any clone regardless of the cloner's autocrlf setting.
*.pickle binary
