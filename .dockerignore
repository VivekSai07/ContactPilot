# Keep the build context lean: outputs, caches, VCS, training-only data
**/__pycache__/
**/*.pyc
**/.git/
**/output/
**/*.gif

# Contact-GraspNet repo: inference needs the package + checkpoints only
contact_graspnet_pytorch/test_data/
contact_graspnet_pytorch/results/
contact_graspnet_pytorch/docs/
contact_graspnet_pytorch/examples/

# Menagerie: only franka_emika_panda is COPY'd, but exclude its VCS anyway
mujoco_menagerie/.git/

# Docker assets themselves
Dockerfile
docker-compose.yml
DOCKER.md
ROADMAP.md
README.md
